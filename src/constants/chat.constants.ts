/** Maximum allowed message length in characters, matching backend limits */
export const CHAT_MAX_MESSAGE_LENGTH = 5000;

/** Rate limit: sending — messages per minute per IP */
export const CHAT_SEND_LIMIT_MAX = 10;
